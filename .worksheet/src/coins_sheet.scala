object coins_sheet {import scala.runtime.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(64); 
  println("Welcome to the Scala worksheet");$skip(22); 
  
  
  var a=List(1);System.out.println("""a  : List[Int] = """ + $show(a ));$skip(12); val res$0 = 
  
  a.tail;System.out.println("""res0: List[Int] = """ + $show(res$0))}
  
}